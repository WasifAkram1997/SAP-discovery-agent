apiVersion: v1
kind: ConfigMap
metadata:
  name: sap-config
  namespace: sap-discovery
data:
  # Non-sensitive configuration
  LANGSMITH_TRACING: "true"
  LANGSMITH_ENDPOINT: "https://api.smith.langchain.com"
  LANGSMITH_PROJECT: "sap-discovery"
  LANGGRAPH_DATABASE_URL: "postgresql://postgres:abcd1234@postgres-service:5432/sap_discovery"